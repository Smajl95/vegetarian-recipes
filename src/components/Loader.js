import React from "react";
import "./Loader.css"; // stile

const Loader = () => {
  return (
    <div className="loader">
      <div className="=spinner"></div>
    </div>
  );
};

export default Loader;
